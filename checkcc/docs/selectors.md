`function myfn(x as Real, with as Real, base as Integer)`

selector:
```
Real_myfn_with_base
myfn(Real, with = ..., base = ...)
```
```
os.myfn(Real, with = ..., base = ...)
```

```
These are SELECTORS!

os.chmod(String)
os.read(String)
os.write(String, data = ...)
http.get(String, data = ...)

```